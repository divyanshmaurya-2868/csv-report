{
  "name": "csv-report",
  "version": "1.0.0",
  "description": "CLI tool that reads ecommerce order data from CSV and generates a revenue summary grouped by product category.",
  "main": "src/index.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test tests/**/*.test.js"
  },
  "dependencies": {},
  "devDependencies": {}
}
